This folder holds files that have been superseeded by new versions of code or hardware. Current Prioritron firmware should work on older hardware including the stripboard prototype.

Prioritron1 refers to the first version using the gerbers in this folder
Prioritron2 uses the same code but uses an opamp so that audio can be heard better over headphones. There are some other changes to reduce the BOM length and improve the audio filter.
